import 'package:flutter/material.dart';

class UsersOnlineWidget extends StatelessWidget {
  const UsersOnlineWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
